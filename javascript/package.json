{
  "name": "mlua-server",
  "version": "1.0.0",
  "description": "Node.js wrapper for the mLua language server — handles indexing, caching, and LSP stdio proxying",
  "main": "mlua-server.js",
  "scripts": {
    "start": "node mlua-server.js --stdio"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "license": "MIT"
}
